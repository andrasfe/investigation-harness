"""Scout — investigation harness for Java test-generation target selection.

See SPEC.md (Project Scout) for the full specification. This package is the
*student* side of the teacher/student loop. A parallel Claude Code session
runs as the teacher and watches `<run_dir>/escalations.jsonl`.
"""

__version__ = "0.1.0"
